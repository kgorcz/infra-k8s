{
    "variables": {
        "aws_access_key": "{{env `AWS_ACCESS_KEY_ID`}}",
        "aws_secret_key": "{{env `AWS_SECRET_ACCESS_KEY`}}"
    },
    "builders": [
        {
            "type": "amazon-ebs",
            "access_key": "{{ user `aws_access_key` }}",
            "secret_key": "{{ user `aws_secret_key` }}",
            "region": "us-east-2",
            "source_ami": "ami-07b77860a9b5d26c0",
            "instance_type": "t3a.micro",
            "ssh_username": "admin",
            "ami_name": "k8s 1.20"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "script": "base.sh"
        }
    ]
  }
  
